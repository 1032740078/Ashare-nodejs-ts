export interface StockData {
    time: Date;
    open: number;
    close: number;
    high: number;
    low: number;
    volume: number;
}
export interface StockInfo {
    code: string;
    name: string;
}
